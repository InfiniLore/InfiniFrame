/**
 * Invoke feature contract. Reserved for direct method invocation on the native host.
 * JavaScript messages execute through the native window's message dispatch;
 * managed delegates cannot be represented across the web-message boundary.
 * @module InvokeInfiniFrameWindowFeature
 */

// ---------------------------------------------------------------------------------------------------------------------
// Imports
// ---------------------------------------------------------------------------------------------------------------------

// ---------------------------------------------------------------------------------------------------------------------
// Code
// ---------------------------------------------------------------------------------------------------------------------

/**
 * Invoke feature API for the InfiniFrame window.
 * Currently a placeholder interface as invoke operations are handled via web messaging.
 */
export interface InvokeInfiniFrameWindowFeature {
}
