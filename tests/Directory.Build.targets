<?xml version='1.0' encoding='utf-8'?>
<Project>
    <Import Project="$(MSBuildThisFileDirectory)../InfiniFrame.NativeCopy.targets"/>

    <PropertyGroup Condition="'$(IsTestingPlatformApplication)' == 'true' and '$(InfiniFrameUseMacOsTestingPlatformHost)' == '' and $([MSBuild]::IsOSPlatform('OSX'))">
        <InfiniFrameUseMacOsTestingPlatformHost>true</InfiniFrameUseMacOsTestingPlatformHost>
    </PropertyGroup>

    <PropertyGroup Condition="'$(InfiniFrameUseMacOsTestingPlatformHost)' == 'true'">
        <GenerateTestingPlatformEntryPoint>false</GenerateTestingPlatformEntryPoint>
        <DefineConstants>$(DefineConstants);INFINIFRAME_MACOS_TEST_HOST</DefineConstants>
        <NoWarn>$(NoWarn);TUnit0034</NoWarn>
    </PropertyGroup>

    <ItemGroup Condition="'$(IsTestingPlatformApplication)' == 'true'">
        <Compile Include="$(MSBuildThisFileDirectory)TestHost/MacOsTestingPlatformEntryPoint.cs" Link="TestHost/MacOsTestingPlatformEntryPoint.cs"/>
    </ItemGroup>
</Project>
