name: "Shared: Linux Tests"
on:
    workflow_call:
        inputs:
            pr_number:
                type: string
                required: true
            checkout_ref:
                type: string
                default: ''
                required: false
            target_sha:
                type: string
                required: true
            workflow_url:
                type: string
                required: true

jobs:
<<<<<<< Updated upstream:.github/workflows/shared-testing-linux.yml
    linux:
        name: Linux (${{ matrix.arch }})
=======
<<<<<<< Updated upstream:.github/workflows/shared-testing-gui-linux.yml
    linux-gui:
        name: Linux GUI (${{ matrix.arch }})
=======
    linux:
        name: CI Testing - Linux (${{ matrix.arch }})
>>>>>>> Stashed changes:.github/workflows/shared-testing-linux.yml
>>>>>>> Stashed changes:.github/workflows/shared-testing-gui-linux.yml
        runs-on: ${{ matrix.os }}
        env:
            TARGET_SHA: ${{ inputs.target_sha }}
            WORKFLOW_URL: ${{ inputs.workflow_url }}
        strategy:
            fail-fast: false
            matrix:
                include:
                    -   os: ubuntu-latest
                        arch: x64
                        libName: linux
                        rid: linux-x64
                    -   os: ubuntu-24.04-arm
                        arch: arm64
                        libName: linux
                        rid: linux-arm64
        permissions:
            contents: read
            statuses: write
            checks: write
        steps:
            -   uses: actions/checkout@v6
                with:
                    ref: ${{ inputs.checkout_ref != '' && inputs.checkout_ref || (inputs.pr_number != '' && inputs.pr_number != null && format('refs/pull/{0}/merge', inputs.pr_number) || github.ref) }}
                    fetch-depth: 1

            -   name: Set Linux Check Pending
                env:
                    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                # noinspection UndefinedAction
                uses: ./.github/actions/sync-check
                # noinspection UndefinedParamsPresent
                with:
                    repo: ${{ github.repository }}
                    sha: ${{ inputs.target_sha }}
                    context: CI Testing - Linux/${{ matrix.arch }}
                    state: pending
                    status-description: Linux tests in progress
                    target-url: ${{ inputs.workflow_url }}
                    allow-status-422: 'true'

            -   name: Setup .NET
                uses: actions/setup-dotnet@v5
                with:
                    dotnet-version: |
                        8.x
                        9.x
                        10.x

            -   name: Cache NuGet
                uses: actions/cache@v5
                with:
                    path: ~/.nuget/packages
                    key: ${{ matrix.os }}-${{ matrix.arch }}-nuget-${{ hashFiles('**/*.csproj', '**/*.props', '**/Directory.Packages.props') }}
                    restore-keys: |
                        ${{ matrix.os }}-${{ matrix.arch }}-nuget-

            -   name: Setup Native dependencies
                uses: ./.github/actions/setup-dependencies-native
                # noinspection UndefinedParamsPresent
                with:
                    apt-cache-version: 1.0
                    brew-cache-key: ${{ matrix.os }}-${{ matrix.arch }}-brew-native-${{ hashFiles('.github/actions/setup-dependencies-native/action.yml', '.github/workflows/shared-testing-linux.yml') }}
                    brew-restore-key: ${{ matrix.os }}-${{ matrix.arch }}-brew-native-

            -   name: Compile GSettings schemas
                run: sudo glib-compile-schemas /usr/share/glib-2.0/schemas/

            -   name: Restore
                run: dotnet restore InfiniFrame.GitHubActions.Testing.slnf /p:NoWarn=NU1503

            -   name: Build Release
                run: |
                    dotnet build src/InfiniFrame.Native/InfiniFrame.Native.proj \
                      --configuration Release \
                      --no-restore \
                      /p:SolutionDir="${{ github.workspace }}/" \
                      /p:Platform=${{ matrix.arch }}

                    dotnet build InfiniFrame.GitHubActions.Testing.slnf \
                      --configuration Release \
                      --no-restore

            -   name: Verify Native Binaries
                uses: ./.github/actions/validate-native-test-binaries

            -   name: Export Actions Runtime
                uses: actions/github-script@v9
                with:
                    script: |
                        core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env['ACTIONS_RUNTIME_TOKEN']);
                        core.exportVariable('ACTIONS_RESULTS_URL', process.env['ACTIONS_RESULTS_URL']);

            -   name: Run Tests
                run: |
                    echo "Starting desktop environment..."

                    eval "$(dbus-launch --sh-syntax)"

                    echo "Launching Xvfb..."
                    Xvfb :99 \
                      -screen 0 1920x1080x24 \
                      -ac \
                      +extension GLX \
                      +extension RANDR \
                      +extension RENDER \
                      -nolisten tcp \
                      -noreset &> xvfb.log &

                    export DISPLAY=:99
                    export XDG_RUNTIME_DIR="/tmp/runtime-$USER"
                    export XDG_SESSION_TYPE=x11
                    export XDG_SESSION_CLASS=user
                    export XDG_SESSION_DESKTOP=ubuntu
                    export XDG_CURRENT_DESKTOP=ubuntu:GNOME
                    export DESKTOP_SESSION=ubuntu

                    mkdir -p "$XDG_RUNTIME_DIR"
                    chmod 700 "$XDG_RUNTIME_DIR"

                    echo "Waiting for X server..."
                    timeout 30 bash -c 'until xdpyinfo >/dev/null 2>&1; do sleep 1; done' || {
                      echo "X server failed to start."
                      exit 1
                    }

                    echo "Starting Mutter window manager..."
                    mutter --x11 --replace --sm-disable &> mutter.log &

                    timeout 20 bash -c 'until pgrep -x mutter >/dev/null; do sleep 1; done' || {
                      echo "Mutter failed to start or crashed."
                      echo "=== Diagnostic: X11 info ==="
                      xdpyinfo | head -3 || echo "xdpyinfo failed"
                      echo "=== Diagnostic: D-Bus ==="
                      echo "$DBUS_SESSION_BUS_ADDRESS"
                      exit 1
                    }

                    echo "Mutter is running:"
                    ps aux | grep "[m]utter" || echo "Mutter missing unexpectedly"

                    timeout 5 xwininfo -root >/dev/null 2>&1 && echo "Window manager responding" || {
                      echo "Window manager not responding"
                      exit 1
                    }

                    echo "=== Environment Status ==="
                    echo "Display: $DISPLAY"
                    echo "Desktop: $XDG_CURRENT_DESKTOP"
                    echo "Session: $XDG_SESSION_DESKTOP"
                    echo "D-Bus: $DBUS_SESSION_BUS_ADDRESS"
                    xdpyinfo | head -3 || true

                    echo "=== Running Tests ==="
                    dotnet test --solution InfiniFrame.GitHubActions.Testing.slnf \
                      --configuration Release \
                      --no-build \
                      --no-restore

            -   name: Pack Tool E2E
                if: always()
                # noinspection UndefinedAction
                uses: ./.github/actions/packtool-e2e
                # noinspection UndefinedParamsPresent
                with:
                    project: examples/InfiniFrameExample.SingleFileExe/InfiniFrameExample.SingleFileExe.csproj
                    rid: ${{ matrix.rid }}
                    output: artifacts/pack-e2e/InfiniFrameExample.SingleFileExe
                    main-output-name: InfiniFrameExample.SingleFileExe
                    forbidden-entries: |
                        wwwroot
                        InfiniFrame.Native.dll
                        WebView2Loader.dll
                        InfiniFrame.Native.so
                        InfiniFrame.Native.dylib

            -   name: Update Linux Check
                if: always()
                env:
                    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                # noinspection UndefinedAction, UndefinedParamsPresent
                uses: ./.github/actions/sync-check-finalize
                # noinspection UndefinedParamsPresent
                with:
                    repo: ${{ github.repository }}
                    sha: ${{ inputs.target_sha }}
                    context: CI Testing - Linux/${{ matrix.arch }}
                    target-url: ${{ inputs.workflow_url }}
                    job-status: ${{ job.status }}
                    success-description: Linux tests passed
                    failure-description: Linux tests failed
